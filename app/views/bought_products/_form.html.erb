<%= form_with(model: bought_product, local: true) do |form| %>
  <% if bought_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bought_product.errors.count, "error") %> prohibited this bought_product from being saved:</h2>

      <ul>
      <% bought_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
